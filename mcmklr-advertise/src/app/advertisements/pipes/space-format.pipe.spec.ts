import { SpaceFormatPipe } from './space-format.pipe';

describe('SpaceFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
